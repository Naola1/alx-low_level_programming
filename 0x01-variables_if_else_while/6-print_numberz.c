#include <stdio.h>
/**
 *main-function that prints a single digit number using putchar
 *Return:success always 0 return
 **/
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
{
		putchar (i);
}
		putchar (10);
	return (0);
}
